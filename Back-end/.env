# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=smart_health

# Environment (for different environments like development or production)
NODE_ENV=development   # Change to "production" for production environment

# Authentication (JWT Secret)
JWT_SECRET=my-secret-key   # Change this to a complex and secure secret key

# Logging Level
LOG_LEVEL=debug   # Change to "info" or "error" for different logging levels

# Optional: Third-Party API Keys or Email Service Credentials
# EMAIL_HOST=smtp.example.com
# EMAIL_USER=your-email@example.com
# EMAIL_PASSWORD=your-email-password
